using Microsoft.Maui.Controls;

namespace Vok.Maui;

/// <summary>Defines the MAUI shell containing the application navigation host.</summary>
public partial class AppShell : Shell {
    /// <summary>Initializes the shell.</summary>
    public AppShell() {
        InitializeComponent();
    }
}
