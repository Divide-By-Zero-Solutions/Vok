# Copilot Instructions

## Project Guidelines
- User prefers a modern, visually polished UI and standardized coding conventions with descriptive variable and method names.

## Performance Attributes
- Use `PerfCritcalAttribute` and `PerfIgnoreAttribute` to support both method-level and class-level usage via `AttributeUsage`.